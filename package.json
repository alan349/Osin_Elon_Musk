{
  "name": "static-html-server",
  "version": "1.0.0",
  "description": "A simple Node.js server to host static HTML files on Railway and handle redirects.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
